#pragma once
#ifndef LUKAN_MATH
#define LUKAN_MATH

#include <iostream>

#include <glm/glm.hpp>

namespace Lk {
    using vint = int;
    using vuint = uint32_t;
    using vfloat = float;
    using vufloat = double;
    using vlong = long;
    using vulong = uint64_t;
    using vshort = short;
    using vushort = uint16_t;
    using vchar = char;
    using vstring = char*;
    using vbool = bool;
};

#endif